{
  "name": "client",
  "version": "1.0.0",
  "main": "./dist/public/index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pre-build-shared": "copyfiles  -V -E -u 2 \"../shared/**/*.ts\" shared",
    "pre-build": "npm run pre-build-shared",
    "post-build": "",
    "dev": "nodemon",
    "build": "npm run pre-build && tsc -b && npm run post-build"
  },
  "author": "Linton Fernandes",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^22.5.4",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "socket.io-client": "^4.7.5"
  }
}
