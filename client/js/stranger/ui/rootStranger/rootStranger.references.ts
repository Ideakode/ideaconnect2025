/**
 * @file rootStranger.references.ts
 * @class rootStrangerReferences
 *
 * @description
 * Extends rootReferences from the base UI module.
 * Inherits the IDs and classes maps (e.g. root_container ID)
 * used when creating and querying the root container element.
 *
 * @see root.rootReferences (base)
 */
import { root } from "../../../base/ui/ui.js";

export class rootStrangerReferences extends root.rootReferences {}
